<%args>
	$jobid
	$infile
</%args>

<%perl>
my $job = ISGA::Job->new(Id => $jobid);
$job->getCreatedBy == ISGA::Login->getAccount or X::User::Denied->throw;
my $status = $job->getStatus;

if($status eq "Finished"){
	return $m->comp('/WorkBench/Results/protdist.mas', infile => $infile);
} elsif ($status eq "Running" || $status eq "Pending"){
	return $m->comp('/WorkBench/Results/running.mas');
}
</%perl>


