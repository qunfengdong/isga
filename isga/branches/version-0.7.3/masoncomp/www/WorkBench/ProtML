<%args>
	$infile
</%args>
<%perl> # -*- cperl -*-

# check that we have a form cached and it is the correct type
my $form = $SysMicro::WebApp->saved_args->{'form'};
$form and $form->get_formname() eq 'workbench_protml'
	or $form =  SysMicro::FormEngine::WorkBench->ProtML(\%ARGS);

return $m->comp('/WorkBench/protml.mas', form => $form, infile => $infile);
</%perl>


