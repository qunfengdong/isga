<%args>
	$infile
</%args>
<%perl> # -*- cperl -*-

return $m->comp('/WorkBench/outage.mas') unless( ISGA::Variable->value('allow_sge_requests') );

# check that we have a form cached and it is the correct type
my $form = $ISGA::WebApp->saved_args->{'form'};
$form and $form->get_formname() eq 'workbench_protdist'
	or $form =  ISGA::FormEngine::WorkBench->ProtDist(\%ARGS);

return $m->comp('/WorkBench/protdist.mas', form => $form, infile => $infile);
</%perl>


