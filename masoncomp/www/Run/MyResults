<%perl> # -*- cperl -*-

  my $runs = SysMicro::Run->query( CreatedBy => SysMicro::Login->getAccount, 
				   OrderBy => 'CreatedAt' );
  foreach my $run (@{$runs}){
    $run->updateStatus();
  }
</%perl>

<& /Run/my_results.mas, runs => $runs &>
